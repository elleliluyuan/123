<!-- pages/add-pet/index.wxml -->
<view class="container">
  <!-- 页面头部 -->
  <view class="page-header">
    <view class="header-left" catchtap="goBack">
      <text class="back-arrow">‹</text>
    </view>
    <text class="page-title">添加宠物</text>
    <view class="header-right"></view>
  </view>

  <!-- 表单内容 -->
  <scroll-view class="form-content" scroll-y="true" enhanced="true" show-scrollbar="false">
    
    <!-- 宠物头像 -->
    <view class="form-card">
      <view class="form-row" catchtap="uploadAvatar">
        <text class="form-label">宠物头像</text>
        <view class="avatar-upload">
          <image wx:if="{{petAvatar}}" src="{{petAvatar}}" class="avatar-image"></image>
          <view wx:else class="avatar-placeholder">
            <text class="camera-icon">📷</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 宠物姓名 -->
    <view class="form-card">
      <view class="form-row">
        <text class="form-label">宠物姓名</text>
        <view class="form-value">
          <input 
            class="input-field" 
            placeholder="输入宠物姓名" 
            value="{{petName}}" 
            bindinput="onNameInput"
            maxlength="20"
          />
        </view>
      </view>
    </view>

    <!-- 宠物品种 -->
    <view class="form-card">
      <view class="form-row" catchtap="selectBreed">
        <text class="form-label">宠物品种</text>
        <view class="form-value">
          <text class="value-text" wx:if="{{petBreed}}">{{petBreed}}</text>
          <text class="placeholder-text" wx:else>选择品种</text>
          <text class="arrow">></text>
        </view>
      </view>
    </view>

    <!-- 宠物性别 -->
    <view class="form-card">
      <view class="form-row">
        <text class="form-label">宠物性别</text>
        <view class="gender-selector">
          <view 
            class="gender-option {{petGender === '公' ? 'selected' : ''}}"
            catchtap="selectGender"
            data-gender="公"
          >
            <text class="gender-icon">♂</text>
          </view>
          <view 
            class="gender-option {{petGender === '母' ? 'selected female' : ''}}"
            catchtap="selectGender"
            data-gender="母"
          >
            <text class="gender-icon">♀</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 宠物生日 -->
    <view class="form-card">
      <view class="form-row">
        <text class="form-label">宠物生日</text>
        <view class="form-value">
          <picker mode="date" value="{{birthDate}}" bindchange="onBirthDateChange">
            <text class="value-text" wx:if="{{birthDate}}">{{birthDate}}</text>
            <text class="placeholder-text" wx:else>选择宠物生日</text>
          </picker>
        </view>
      </view>
    </view>

    <!-- 宠物体重 -->
    <view class="form-card">
      <view class="form-row">
        <text class="form-label">宠物体重</text>
        <view class="form-value">
          <input 
            class="input-field" 
            placeholder="填写宠物体重" 
            value="{{petWeight}}" 
            bindinput="onWeightInput"
            type="digit"
            maxlength="5"
          />
          <text class="unit-text">kg</text>
        </view>
      </view>
    </view>

    <!-- 备注 -->
    <view class="form-card">
      <view class="form-row">
        <text class="form-label">备注</text>
        <view class="checkbox-group">
          <view 
            class="checkbox-item {{isNeutered ? 'checked' : ''}}"
            catchtap="toggleNeutered"
          >
            已绝育
          </view>
          <view 
            class="checkbox-item {{isVaccinated ? 'checked' : ''}}"
            catchtap="toggleVaccinated"
          >
            已打疫苗
          </view>
        </view>
      </view>
    </view>

  </scroll-view>

  <!-- 保存按钮 -->
  <button class="save-button" catchtap="savePet">保存</button>
</view>