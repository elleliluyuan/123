/* pages/statistics/index.wxss */
.container {
  min-height: 100vh;
  background: var(--bg-warm-creamy);
}

.page-header {
  padding: 60rpx 40rpx 40rpx;
  text-align: center;
}

.page-title {
  font-size: 48rpx;
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1.2;
}

.content {
  padding: 100rpx 40rpx;
  text-align: center;
}

.placeholder-text {
  font-size: 32rpx;
  color: var(--text-secondary);
  font-weight: 500;
}